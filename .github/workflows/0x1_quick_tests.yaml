# Fork: https://github.com/shutingrz/busybox-static-binaries-fat/tree/main
name: 0x1 ➼ Quick Tests

on:
  workflow_dispatch:

env:
  GITHUB_TOKEN: "${{ secrets.STATIC_BINARIES_RELEASER }}"

jobs:
  build:
    runs-on: ubuntu-22.04

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3
        with:
          path: main
          
          
  #Create a new Release & Publish
  # Repo: https://github.com/softprops/action-gh-release
  # Market-Place: https://github.com/marketplace/actions/gh-release
      - name: Releaser
        uses: softprops/action-gh-release@v0.1.15
        with:
          name: "Tailscale ${{ env.VERSION }}"
          tag_name: "tailscale"
          body: "$GITHUB_WORKSPACE/main/tailscale/INFO.md"
          prerelease: false
          draft: false
          generate_release_notes: false
          token: "${{ secrets.GITHUB_TOKEN }}"
          files: |
            ${{github.workspace}}/tailscale/*_Linux
            ${{github.workspace}}/tailscale/*_macOS
            ${{github.workspace}}/tailscale/*.exe
